# ===== v3.17+ 精簡依賴清單 =====
# SelfLearningTrader 絕對控制權 + 無限制槓桿
# 僅保留核心依賴，移除所有深度學習框架

# ===== 核心依賴 =====
aiohttp>=3.9.1              # 異步 HTTP 客戶端（Binance API）
ccxt>=4.2.0                 # 加密貨幣交易所統一 API
pandas>=2.0.0               # 數據處理與分析
numpy>=1.24.0,<2.0.0        # 數值計算（兼容 XGBoost）
python-dotenv>=1.0.0        # 環境變量管理
websockets>=12.0            # WebSocket 客戶端（即時市場數據）

# ===== ML 依賴（僅 XGBoost）=====
xgboost>=2.0.0              # 唯一 ML 模型（輕量、高效）
scikit-learn>=1.3.0         # ML 工具（特徵工程、評估指標）

# ===== 系統監控 =====
psutil>=5.9.0               # 系統資源監控

# ===== 異步 I/O =====
aiofiles                    # 異步文件讀寫（報告生成）

# ===== 通知（可選）=====
discord.py>=2.3.0           # Discord 通知

# ===== 說明 =====
# v3.17+ 移除的依賴：
# - tensorflow>=2.13.0          # ❌ 深度學習框架（已棄用）
# - tensorflow-addons>=0.19.0   # ❌ TensorFlow 擴展（已棄用）
# - onnxmltools>=1.11.0         # ❌ ONNX 轉換工具（已棄用）
# - onnxruntime>=1.18.0         # ❌ ONNX 推理引擎（已棄用）
#
# 總依賴數：10 個（從 14 個減少）
# 安裝體積：~500MB（從 ~2GB 減少）
# 啟動時間：~5 秒（從 ~15 秒減少）
