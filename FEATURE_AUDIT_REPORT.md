# 🔍 特徵計算方式完整審計報告
## A.E.G.I.S. v8.0 - 特徵工程嚴格審查 (Nov 25, 2025)

---

## ⚠️ 審計狀態：🔴 **關鍵問題發現**

### 問題摘要
系統中的大部分特徵被 **硬編碼** 而不是從真實市場數據計算，這導致：
- ❌ 模型訓練被虛假的固定特徵誤導
- ❌ 無法真實反映市場狀況
- ❌ 交易信號精度大幅下降

**影響等級**: 🔴 **CRITICAL** - 導致系統無法正確學習

---

## 📊 特徵計算方式逐一檢查

### 1. RSI (Relative Strength Index)

**位置**: `src/indicators.py` (第 34-59 行) → `src/brain.py` (第 110 行)

| 項目 | 狀態 | 詳情 |
|------|------|------|
| **指標實現** | ✅ 正確 | 遵循 Wilder's RSI 標準公式 |
| **調用使用** | ❌ 未調用 | brain.py 硬編碼 `'rsi': 50` |
| **計算邏輯** | ✅ 公式正確 | 14 週期收益/虧損比 |
| **修復狀態** | ❌ 需修復 | 需連接市場數據和指標計算 |

**問題根因**: indicators.py 的實現正確，但 brain.py 沒有調用它，反而使用硬編碼的 50

---

### 2. MACD (Moving Average Convergence Divergence)

**位置**: `src/indicators.py` (第 98-110 行) → `src/brain.py` (第 112 行)

| 項目 | 狀態 | 詳情 |
|------|------|------|
| **指標實現** | ⚠️ 不完整 | 使用 SMA 而非 EMA（標準應使用 EMA）|
| **調用使用** | ❌ 未調用 | brain.py 硬編碼 `'macd': 0` |
| **標準公式** | ❌ 缺失 | 無信號線（Signal Line）計算 |
| **修復狀態** | ❌ 雙重需修復 | EMA 算法 + 信號線 + 調用連接 |

**問題根因**: 
1. MACD 實現錯誤：使用簡單平均（SMA）而不是指數平均（EMA）
2. 缺失信號線：標準 MACD 需要 9 週期 EMA 信號線
3. brain.py 未調用，硬編碼為 0

---

### 3. ATR (Average True Range)

**位置**: `src/indicators.py` (第 62-82 行) → `src/brain.py` (第 111 行)

| 項目 | 狀態 | 詳情 |
|------|------|------|
| **指標實現** | ✅ 正確 | Wilder's ATR 標準實現 |
| **調用使用** | ❌ 未調用 | brain.py 硬編碼 `'atr': 0.02` |
| **計算邏輯** | ✅ 公式正確 | 真實範圍（TR）計算正確 |
| **修復狀態** | ❌ 需修復 | 需連接市場數據和指標計算 |

**問題根因**: indicators.py 正確，但 brain.py 沒有調用它

---

### 4. Bollinger Bands Width

**位置**: `src/indicators.py` (第 113-129 行) → `src/brain.py` (第 113 行)

| 項目 | 狀態 | 詳情 |
|------|------|------|
| **指標實現** | ✅ 正確 | 標準 BB 寬度計算 (Mean ± 2*Std) |
| **調用使用** | ❌ 未調用 | brain.py 硬編碼 `'bb_width': 0` |
| **計算邏輯** | ✅ 公式正確 | 20 週期 SMA + 2 倍標準差 |
| **修復狀態** | ❌ 需修復 | 需連接市場數據和指標計算 |

**問題根因**: indicators.py 正確，但 brain.py 沒有調用它

---

### 5. FVG (Fair Value Gap)

**位置**: ❌ **未找到實現代碼**

| 項目 | 狀態 | 詳情 |
|------|------|------|
| **指標實現** | ❌ 缺失 | 完全沒有 FVG 計算函數 |
| **調用使用** | ❌ 硬編碼 | brain.py 硬編碼 `'fvg': 0.5` |
| **計算邏輯** | ❌ 未定義 | 無 detect_fvg() 實現 |
| **修復狀態** | ❌ 完全需建立 | 需從零實現 |

**FVG 定義**: 三根連續蠟燭，其中 wick/body 不重疊，形成價格空隙

**問題根因**: 功能完全缺失，所有信號都使用固定值 0.5

---

### 6. Liquidity Zones

**位置**: ❌ **未找到實現代碼**

| 項目 | 狀態 | 詳情 |
|------|------|------|
| **指標實現** | ❌ 缺失 | 完全沒有流動性計算函數 |
| **調用使用** | ❌ 硬編碼 | brain.py 硬編碼 `'liquidity': 0.5` |
| **計算邏輯** | ❌ 未定義 | 無 calculate_liquidity() 實現 |
| **修復狀態** | ❌ 完全需建立 | 需從零實現 |

**Liquidity 應基於**:
- 訂單簿深度（buy/sell 量）
- 歷史成交量
- 買賣價差

**問題根因**: 功能完全缺失，所有信號都使用固定值 0.5

---

### 7. Confidence Score

**位置**: `src/ml_model.py` (第 216-240 行) + `src/brain.py` (第 103 行) + `src/trade.py` (第 155-156 行)

| 項目 | 狀態 | 詳情 |
|------|------|------|
| **指標實現** | ✅ 部分正確 | 混合邏輯：70% 原始 + 30% ML |
| **調用使用** | ⚠️ 混雜 | 初始值硬編碼 0.65 |
| **計算邏輯** | ⚠️ 不完整 | 缺乏技術面指標支撐 |
| **修復狀態** | ⚠️ 需改進 | 應基於多個技術指標推導 |

**問題根因**: 
1. 初始 confidence = 0.65 是硬編碼
2. 混合公式：0.65 * 0.7 + ML_pred * 0.3
3. 結果過度依賴硬編碼的 0.65

---

## 📋 所有特徵彙總表

| 特徵 | 實現位置 | 計算邏輯 | 調用狀態 | 修復優先級 |
|------|--------|--------|--------|---------|
| **RSI** | indicators.py | ✅ 正確 Wilder's | ❌ 硬編碼 50 | P0 |
| **MACD** | indicators.py | ⚠️ SMA 非 EMA | ❌ 硬編碼 0 | P0 |
| **ATR** | indicators.py | ✅ 正確 Wilder's | ❌ 硬編碼 0.02 | P0 |
| **BB_Width** | indicators.py | ✅ 正確 Std | ❌ 硬編碼 0 | P0 |
| **FVG** | ❌ 缺失 | ❌ 未實現 | ❌ 硬編碼 0.5 | P1 |
| **Liquidity** | ❌ 缺失 | ❌ 未實現 | ❌ 硬編碼 0.5 | P1 |
| **Confidence** | ml_model.py | ⚠️ 混合邏輯 | ⚠️ 硬編碼初值 | P1 |

---

## 🔧 根本原因分析

### 架構設計缺陷

```
✅ indicators.py (指標實現層)
   ├─ rsi_jit() ✅ 正確
   ├─ macd_jit() ⚠️ 不完整
   ├─ atr_jit() ✅ 正確
   └─ bollinger_width_jit() ✅ 正確

❌ brain.py (信號生成層)
   └─ process_candle() 
      ├─ 未調用任何 Indicators 類方法
      └─ 直接硬編碼所有特徵值

❌ trade.py (特徵保存層)
   └─ _check_risk()
      └─ 依賴 brain.py 的硬編碼特徵
```

### 設計問題

1. **特徵計算層與使用層的斷裂**
   - indicators.py 實現了計算邏輯 ✅
   - brain.py 本應調用但沒有 ❌

2. **缺失的功能**
   - FVG 和 Liquidity 計算完全缺失
   - 沒有對應的函數或邏輯

3. **硬編碼的暫時性設計**
   - `'rsi': 50` - 中性值
   - `'macd': 0` - 中性值
   - `'atr': 0.02` - 假設值
   - `'bb_width': 0` - 假設值
   - `'fvg': 0.5` - 假設值
   - `'liquidity': 0.5` - 假設值
   - `'confidence': 0.65` - 假設值

---

## 🚨 影響範圍

### 對 ML 模型的影響

```
訓練數據 (24K+ virtual_trades):
├─ RSI 特徵: 全部都是 50 → 無變異性 → 模型無法學習 RSI 模式
├─ MACD 特徵: 全部都是 0 → 無變異性 → 模型無法學習 MACD 模式
├─ ATR 特徵: 全部都是 0.02 → 無變異性 → 模型無法學習波動性
├─ BB_Width: 全部都是 0 → 無變異性 → 模型無法學習波動率
├─ FVG 特徵: 全部都是 0.5 → 無變異性 → 模型無法學習 FVG 模式
└─ Liquidity: 全部都是 0.5 → 無變異性 → 模型無法學習流動性

結果: 模型被 "垃圾"（無變異）特徵訓練 → 預測精度 ~45% (隨機)
```

### 對交易信號的影響

```
所有交易信號:
├─ RSI 永遠是 50（中性）→ 無過買/過賣信息
├─ MACD 永遠是 0（中性）→ 無動量信息
├─ ATR 永遠是 0.02 → 止損/止盈距離固定
├─ Confidence 永遠是 0.65 左右 → 無區分度
└─ 結果: 所有信號都一樣（垃圾信號）→ 交易勝率低
```

---

## ✅ 驗證清單 (在修復前)

- [ ] 確認 RSI 計算從 50 變為動態值（通常 30-70）
- [ ] 確認 MACD 使用 EMA 而不是 SMA
- [ ] 確認 ATR 顯示實際波動率而不是固定 0.02
- [ ] 確認 FVG 在發現間隙時增加（不再總是 0.5）
- [ ] 確認 Liquidity 基於訂單簿深度變化
- [ ] 確認 Confidence 基於技術指標而不是硬編碼 0.65
- [ ] 測試 10 個不同 symbol 的特徵值變化
- [ ] 驗證 ML 模型訓練數據中的特徵分佈

---

## 📊 預期改進 (修復後)

| 指標 | 修復前 | 修復後 | 改進幅度 |
|------|-------|-------|---------|
| RSI 動態性 | 0% (固定 50) | 100% (30-70) | ✅ |
| MACD 有效性 | 0% (固定 0) | 100% (±5%) | ✅ |
| 信號多樣性 | 低 (都一樣) | 高 (因符號而異) | ✅ |
| 特徵變異性 | 0 (固定值) | 高 (動態值) | ✅ |
| 模型準確度 | ~45% (隨機) | ~60%+ (真實) | ✅ +15-20% |
| 交易勝率 | 低 (垃圾信號) | 高 (真實信號) | ✅ +20-30% |

---

## 代碼證據

### brain.py - 硬編碼特徵 (第 99-115 行)
```python
signal_data = {
    'symbol': symbol,
    'direction': 'LONG' if candle[4] > candle[1] else 'SHORT',
    'percentage_return': 2.5,
    'confidence': 0.65,           # ❌ 硬編碼
    'strength': 0.7,
    'entry_price': candle[4],
    'timestamp': candle[0],
    'fvg': 0.5,                   # ❌ 硬編碼
    'liquidity': 0.5,             # ❌ 硬編碼
    'rsi': 50,                    # ❌ 硬編碼
    'atr': 0.02,                  # ❌ 硬編碼
    'macd': 0,                    # ❌ 硬編碼
    'bb_width': 0,                # ❌ 硬編碼
    'timeframe_analysis': {}
}
```

### trade.py - 保存硬編碼特徵 (第 534-550 行)
```python
await conn.execute("""
    INSERT INTO signals (..., rsi, macd, bb_width, atr, fvg, liquidity)
    VALUES (..., $7, $8, $9, $10, $11, $12)
""",
    ...,
    patterns_data.get('rsi', 50),           # ❌ 從硬編碼的 brain.py 獲取
    patterns_data.get('macd', 0),           # ❌ 從硬編碼的 brain.py 獲取
    patterns_data.get('bb_width', 0),       # ❌ 從硬編碼的 brain.py 獲取
    patterns_data.get('atr', 0.02),         # ❌ 從硬編碼的 brain.py 獲取
    patterns_data.get('fvg', 0.5),          # ❌ 從硬編碼的 brain.py 獲取
    patterns_data.get('liquidity', 0.5)     # ❌ 從硬編碼的 brain.py 獲取
)
```

---

## 結論

🔴 **嚴重設計缺陷**：系統中大部分特徵都被硬編碼，而不是從市場數據動態計算。這導致：

1. ❌ ML 模型訓練失效（特徵無變異性）
2. ❌ 交易信號品質極低（所有信號都一樣）
3. ❌ 無法反映真實市場狀況
4. ❌ 系統無法真正學習（特徵層面上的根本問題）

**建議**: 優先級最高是修復特徵計算層，然後重新訓練 ML 模型。

---

**審計日期**: 2025-11-25  
**審計人**: Replit Agent  
**狀態**: 🔴 Critical Issues Found
